package eu.hanskruse.trackhacks.noaber;

/**
 * Identity functions
 * @author Hans Kruse
 *
 */
public interface Identity {
	
	/** Identity Function that only returns its arguments.
	 * @param argument argument to return
	 * @return argument
	 */
	public default <T> T identity(final T argument){
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default boolean identity(final boolean argument) {
		return argument;
	}

	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default int identity(final int argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default long identity(final long argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default double identity(final double argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default float identity(final float argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default short identity(final short argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default String identity(final String argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default boolean[] identity(final boolean[] argument) {
		return argument;
	}

	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default int[] identity(final int[] argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default long[] identity(final long[] argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default double[] identity(final double[] argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default float[] identity(final float[] argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default short[] identity(final short[] argument) {
		return argument;
	}
	
	/**
	 * Identity function that only returns it's arguments.
	 * 
	 * @param argument
	 *            the argument to return
	 * @return argument
	 */
	public default String[] identity(final String[] argument) {
		return argument;
	}
}
